import 'package:flutter/material.dart';

class BottomNavSecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
      child: Center(
        child: Text(
          'Call',
          style: TextStyle(color: Colors.white, fontSize: 24),
        ),
      ),
    );
  }
}
